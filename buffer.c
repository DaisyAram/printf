#include "main.h"

/**
 *
 *
 */
printf
{
	char buffer[BUFF_SIZE];
	int buffer_index = 0;

	if (format == NULL)
	return (-1);
	else if (format = '\0')
	break;

	if(format != '%')
	buffer[buffer_index] = format;

	if(format == '%')
	format ++;

	else if (format++ == c || d || s || i)
	char buffer[BUFF_SIZE];
	print_buffer(buffer,
